/**
 * Constants for onion routing implementation
 */

export const CONNECTION_CONSTANTS = {
  WAITING_PERIOD: 30000,  // 30 seconds waiting period
  MAX_RECONNECT_ATTEMPTS: 5,
  RECONNECT_BACKOFF_MS: 1000,
  MIN_NODES_REQUIRED: 2
};
